<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="plan">

	<!-- <select id="getPlan" parameterType="int" resultType="PlanDTO">
		내가 구해야되는 것들 : 일정제목(board), 내용(board), 날짜(board,plan), 장소(plan)
		select p.pbrdno,p.ploc,p.pdate,b.brdcontent,b.title 
		from planboardinfo p, board b where b.userno = #{userno} and b.brdno = p.brdno
	</select>
 -->
</mapper>