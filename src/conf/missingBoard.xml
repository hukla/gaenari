<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="missingboard">
	
	<select id="selectAll" resultMap="brdMap">
		select * from board where brdtype=3
	</select>
	<!-- 
	<select id="selectByNum" resultMap="brdMap" parameterType="int">
		select m.mfbrdno, b.brdno. b.title, b.userid, b.wrdate, b.content, m.mfloc, m.mfdoginfo, m.mftype
		from mfboardinfo m and board b where m.mfbrdno=#{num}
	</select>
	 
	<insert id="insert" parameterType="MfBoardDTO">
		insert into mfboardinfo VALUES(#{mfloc},#{mfdoginfo}, #{mftype})
	</insert>
	
	<update id="updateOne" parameterType="MfBoardDTO">
		 update mfboardinfo set brdno.title=#{brdno.title}, brdno.content=#{brdno.content}, writetime=SYS_DATETIME, mfloc=#{mfloc}, mfdoginfo=#{mfdoginfo}, mftype=#{mftype} where brdno=#{brdno} and writer=#{writer}
	</update>
	
	<delete id="delete"  parameterType="int">
		DELETE FROM mfboardinfo WHERE brdno=#{brdno};
		DELETE FROM board WHERE brdno=#{brdno};
	</delete>
	-->
</mapper>